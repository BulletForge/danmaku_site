- if can? :update, @project
  .mb-2
    .mb-2
      = link_to "Edit Project", edit_user_project_path(@user, @project), method: :get, class: 'btn btn-secondary'
    .mb-2
      = link_to "Delete Project", user_project_path(@user, @project), method: :delete, data: {confirm: "Are you sure you want to delete this project?"}, class: 'btn btn-danger'
