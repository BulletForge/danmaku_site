<h1>Index of Projects</h1>

<%= will_paginate @projects %>

<% @projects.each do |p| %>
  <%= link_to p.title, user_project_path(p.user, p) %><br />
  by <%= link_to p.user.login, user_path(p.user) %><br />
  <br />
<% end %>

<%= will_paginate @projects %>