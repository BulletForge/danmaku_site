<section class="d-flex text-center flex-column justify-content-center align-items-center">

.page-header
  %h1
    Login

= semantic_form_for @user_session, :url => user_session_path do |f|
  = f.inputs :login, :password
  = f.actions do
    = f.action :submit, :label => "Log In"

</section>
